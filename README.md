# TimeTracker
Entry system for tracking client hours.

Can handle more than one organization, each with its own clients, projects, and users.

Can create invoices with attached timesheets for client submission.

Should be able to be run with Docker. Just run `docker compose up` and log in with admin:admin. From there, setting up organizations, clients, projects, and users should be relatively self-explanatory. 